{
  "title": "Shine.com Jobs Scraper",
  "description": "Scrape jobs from Shine.com using internal JSON endpoints (API-first). Provide either keyword/location OR a Shine.com search URL (startUrl, url, or startUrls). The actor paginates and can optionally fetch job details.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrl": {
      "title": "Start URL",
      "type": "string",
      "description": "Start scraping from a specific Shine.com search URL. If provided, this will be used instead of Keyword/Location.",
      "editor": "textfield"
    },
    "keyword": {
      "title": "Keyword (if not using Start URL)",
      "type": "string",
      "description": "Job search keyword(s), e.g., 'Software Developer', 'Marketing Manager', 'Data Analyst'.",
      "editor": "textfield",
      "prefill": "software engineer"
    },
    "location": {
      "title": "Location (if not using Start URL)",
      "type": "string",
      "description": "Location filter, e.g., 'Mumbai', 'Delhi', 'Bangalore', 'Chennai', 'Pune'.",
      "editor": "textfield",
      "prefill": "Bangalore"
    },
    "collectDetails": {
      "title": "Collect details",
      "type": "boolean",
      "description": "If enabled, fetches job detail data (description, skills, etc.).",
      "editor": "checkbox",
      "default": true
    },
    "datePosted": {
      "title": "Job posting date filter",
      "type": "string",
      "description": "Filter jobs by how recently they were posted.",
      "enum": [
        "anytime",
        "24hours",
        "7days",
        "30days",
        "90days"
      ],
      "enumTitles": [
        "Anytime",
        "Last 24 hours",
        "Last 7 days",
        "Last 30 days",
        "Last 90 days"
      ],
      "default": "anytime",
      "editor": "select"
    },
    "results_wanted": {
      "title": "Maximum number of jobs",
      "type": "integer",
      "description": "The maximum number of jobs to collect. Leave empty to collect all available jobs.",
      "minimum": 1,
      "default": 20,
      "prefill": 20,
      "editor": "number"
    },
    "max_pages": {
      "title": "Maximum number of pages",
      "type": "integer",
      "description": "A safety cap on the number of search result pages to visit.",
      "minimum": 1,
      "editor": "number",
      "default": 20,
      "prefill": 3
    },
    "proxyConfiguration": {
      "title": "Proxy configuration",
      "type": "object",
      "description": "Optional. On Apify platform, the actor enables Apify Proxy automatically if you leave this empty. For local runs, leave this empty to run without Apify Proxy (recommended).",
      "editor": "proxy"
    }
  },
  "required": [],
  "additionalProperties": false
}
